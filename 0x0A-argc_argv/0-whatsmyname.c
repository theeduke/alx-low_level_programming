#include <stdio.h>
/**
 * main - function to print itself
 * @argc: argument counter
 * @argv: argument value
 * Return: 0 (succes)
 */
int main(int argc, char *argv[])
{
	if (agrv[0])
	{
		printf("%d\n", argc - 1");
	}
	return (0);
}
