#include "main.c"
/**
 * _islower - Entry point
 *
 *@c: An input character
 * Return: 0 (success)
 */
int _islower(int c)
{
	char a;
	int lower = 0;

	for (a = 'a'; a <= 'z'; a++)
	{
		if (a == c)
		{
			lower = 1;
		}
	}
	return (b);
}
