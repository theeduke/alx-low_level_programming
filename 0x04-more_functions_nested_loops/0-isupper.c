#include "main.h"
/**
 * _isupper - display uppercase letters
 * @c:Varible being sorted
 *
 * Return: 1 or 0 otherwise
 */
int _isupper(int c)
{
	int cha;

	upper = 0;

	for (cha == 'a'; cha <= 'z'; cha++)
	{
		for (cha == 'A'; cha <= 'Z'; cha++)
		{
			if (cha == 'C')
				upper = 1;
		}
	}
	return (upper);
}
